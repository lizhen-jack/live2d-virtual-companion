# 🎭 Live2D虚拟伴侣 - 使用指南

## 🚀 快速启动

### 步骤1：克隆项目

```bash
git clone https://github.com/lizhen-jack/live2d-virtual-companion.git
cd live2d-virtual-companion
```

### 步骤2：启动本地服务器

**Python方式（推荐）**:
```bash
python3 -m http.server 8080
```

**Node.js方式**:
```bash
npx http-server -p 8080
```

### 步骤3：打开浏览器

访问 http://localhost:8080

---

## 🎯 首次使用

### 1. 允许摄像头和麦克风权限

浏览器会提示是否允许：
- ✅ 摄像头 - 允许（让虚拟人物看到你）
- ✅ 麦克风 - 允许（用于语音识别）

### 2. 点击"开始对话"

虚拟人物会打招呼，然后你就可以：
- 直接对她说话（中文）
- 等待虚拟人物回复
- 观察她的表情和情绪变化

---

## 🎨 功能说明

### 虚拟人物

当前显示的是**占位符**动画。要拥有真实的Live2D虚拟人物，你需要：

#### 选项1：使用免费Live2D模型

1. 下载Live2D官方示例模型
2. 解压到 `assets/live2d-model/`
3. 修改 `js/live2d.js` 中的模型加载逻辑

#### 选项2：创建自己的Live2D模型

1. 下载 [Live2D Cubism Editor](https://www.live2d.com/download/cubism/)
2. 创建或导入角色设计
3. 导出为.moc3和.model3.json文件
4. 上传到项目

### 语音对话

- **语音识别**：自动监听你的说话，转成文字
- **AI对话**：智能理解你的话，生成回复
- **语音合成**：把回复念出来（浏览器原生TTS）

### 情感系统

虚拟人物会根据对话内容自动切换表情：
- 😊 **开心** - 听到夸奖、快乐的事
- 😢 **悲伤** - 听到难过的事
- 😠 **生气** - 听到愤怒的词
- 😮 **惊讶** - 听到意外的事
- 😐 **平静** - 默认状态

### 摄像头互动

- 虚拟人物可以看到你（通过摄像头）
- 眼神自动追踪（基于摄像头位置）
- 隐私保护：摄像头画面不上传服务器

---

## 🔧 高级配置

### 连接OpenAI API

编辑 `js/ai.js`，修改配置：

```javascript
const AI_CONFIG = {
    provider: 'openai',  // 改为openai
    apiKey: 'YOUR_OPENAI_API_KEY',  // 你的API密钥
    model: 'gpt-4'  // 或 gpt-3.5-turbo
};
```

**获取OpenAI API密钥**:
1. 访问 https://platform.openai.com/
2. 注册/登录
3. 进入 "API Keys"
4. 创建新密钥

### 修改语音设置

打开浏览器设置面板（右上角⚙️）：
- 调整语音速度（0.5x - 2x）
- 调整音调（低沉 - 尖锐）
- 切换表情模式（自动/手动）

### 自定义系统提示词

在 `js/ai.js` 中修改 `systemPrompt`：

```javascript
systemPrompt: `你是一个活泼可爱的虚拟少女...
特点：
1. 喜欢开玩笑
2. 说话调皮
3. 记住用户的喜好`
```

---

## 🎭 使用技巧

### 获得更好的对话体验

1. **说普通话**：语音识别对普通话效果最好
2. **保持安静环境**：减少噪音干扰
3. **清晰说话**：不要太快或太慢
4. **给予反馈**：可以夸奖、抱怨、聊天

### 表情互动

- 说"你真棒" → 虚拟人物会开心 😊
- 说"我很难过" → 虚拟人物会安慰 😢
- 说"生气了" → 虚拟人物会道歉 😢

---

## 🚀 常见问题

### Q: 听不到虚拟人物说话？

检查：
1. 浏览器音量是否打开
2. 系统音量是否正常
3. 是否禁用了自动播放（允许页面自动播放声音）

### Q: 语音识别不工作？

解决：
1. 检查麦克风权限
2. 刷新页面重新请求权限
3. 使用Chrome浏览器（兼容性最好）

### Q: 虚拟人物模型不显示？

可能原因：
1. 模型文件路径错误
2. 模型文件格式不对
3. 浏览器不支持WebGL

解决：
- 先使用占位符测试
- 检查浏览器控制台错误信息

---

## 🎁 下一步优化

### 短期优化（1-2天）

1. **添加真实Live2D模型**
   - 下载免费模型
   - 正确配置路径

2. **提升AI对话质量**
   - 连接OpenAI API
   - 优化系统提示词

3. **增加更多互动方式**
   - 支持文字输入
   - 添加表情按钮
   - 添加快捷短语

### 中期优化（1周）

1. **情感AI升级**
   - 分析语调
   - 学习用户喜好
   - 记忆对话历史

2. **虚拟人物动作**
   - 头部转动
   - 手势动作
   - 眨眼动画

3. **多轮对话**
   - 上下文记忆
   - 话题切换
   - 引导对话

### 长期愿景（1月）

1. **完整Live2D模型**
   - 全身动画
   - 姿态变换
   - 服装更换

2. **3D虚拟人物**
   - VRM格式支持
   - 身体动作捕捉
   - 场景互动

3. **深度学习情感**
   - 实时面部表情识别
   - 语音情感分析
   - 用户情绪追踪

---

## 📞 技术支持

如果遇到问题：
1. 检查浏览器控制台（F12）
2. 查看本文档
3. 提交GitHub Issue

---

**创造你的专属虚拟伴侣！** 🦊

*更新时间: 2026-02-20*
*作者: 李祯*
*AI: 小龙*
